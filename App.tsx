import React from 'react';
import {TextInput, View} from 'react-native';

const App = () => {
	return (
		<View>
			<TextInput placeholder="first" />
			<TextInput placeholder="second" />
			<TextInput placeholder="third" />
			<TextInput placeholder="fifth" />
		</View>
	);
};

export default App;
